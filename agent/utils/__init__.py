"""Shared utilities for the agent's LangGraph nodes.

Cross-cutting helpers used by more than one node, kept separate from
node logic itself, starting with LLM call reliability (see
llm_utils.py).

"""
