"""Test suite for the wake-on-request Lambda functions.

This package validates the handlers that start and stop ECS services and
the RDS instance in response to traffic and inactivity, ensuring the
wake-on-request infrastructure behaves correctly under both states.

"""
