# tests/agent/__init__.py
"""Test suite for the agentic layer.

Mirrors the structure of the agent package, one test module per node,
model, or schema, following the same testing conventions already used
for the ML layer.

"""
