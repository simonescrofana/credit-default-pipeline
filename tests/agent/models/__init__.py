# tests/agent/models/__init__.py
"""Test suite for the agent's LLM instantiation.

Covers the factory functions in agent.models, mocking the underlying
chat model classes so no real API call is made during testing.

"""
