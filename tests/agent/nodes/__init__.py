# tests/agent/nodes/__init__.py
"""Test suite for the agent's LangGraph node functions.

Covers the behavior of each node in agent.nodes, mocking LLM calls and
verifying both the returned state update and the message payload built
for the LLM.

"""
