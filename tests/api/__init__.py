"""Test suite for the HTTP API layer.

Mirrors the structure of the `api` package, one test module per endpoint,
dependency, or store, following the same testing conventions already used
for the ML and agentic layers.

"""
