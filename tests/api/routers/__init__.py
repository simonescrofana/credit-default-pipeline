"""Test suite for the FastAPI routers.

Mirrors the structure of the `api.routers` package, one test module per
router, following the same testing conventions already used elsewhere in
`tests.api`.

"""
