"""Data ingestion pipeline for the OLAP architecture.

This package manages the extraction of raw financial and transactional data
from operational databases, handling both the initial data harvesting and
the subsequent restoration. These components serve as the foundational data
layer for the credit default prediction model.

"""
