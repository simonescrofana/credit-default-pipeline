"""Data validation and serialization schemas package initialization.

This package centralizes the Pydantic operational schemas, custom domain types,
and validation layers. It acts as the single source of truth for data integrity
across the pipeline, managing database record hydration for the OLTP layer and
governing request/response serialization for the FastAPI production interface.

"""
